[project]
name = "convert-image-service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn[standard]>=0.30.0",
  "pillow>=10.0.0"
]

[tool.uvicorn]
host = "0.0.0.0"
port = 9002
